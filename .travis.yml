language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls

services:
  - postgresql

before_script:
  - psql -c 'create database send_it;' -U postgres
  - psql -c "CREATE USER postgres WITH PASSWORD 'v0k3ch!97';" -U postgres

script:
  - pytest --cov=app/

after_success:
  - coveralls
